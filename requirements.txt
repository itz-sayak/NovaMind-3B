# ── Core (CUDA 12.4 build — reinstall from scratch if switching CUDA) ─────
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
triton==3.2.0

# ── Attention kernels ─────────────────────────────────────────────────────
# Install these *after* torch:
#   pip install flash-attn --no-build-isolation
#   pip install git+https://github.com/fla-org/flash-linear-attention.git
flash-attn==2.7.2.post1
# flash-linear-attention must be installed from source (PyPI 0.4.1 is missing fla.ops):
#   pip install git+https://github.com/fla-org/flash-linear-attention.git
einops==0.8.2

# ── Data / tokenization ───────────────────────────────────────────────────
numpy==2.4.2
tiktoken==0.12.0
sentencepiece==0.2.1
datasets==4.6.1
pyarrow==23.0.1
tqdm==4.67.3
requests==2.32.5
huggingface_hub==1.6.0
tokenizers==0.22.2
transformers==5.3.0
accelerate==1.11.0
safetensors==0.7.0
fsspec==2026.2.0
multiprocess==0.70.18
dill==0.4.0
aiohttp==3.13.3

# ── Experiment tracking ───────────────────────────────────────────────────
wandb==0.25.0

# ── Numerics / utilities ──────────────────────────────────────────────────
scipy==1.15.3
regex==2026.2.28
packaging==26.0
filelock==3.25.0
networkx>=3.0
